# Strict type analysis for the state-management layer (staged rollout, phase 2).
include: ../../../../analysis_options.yaml

analyzer:
  language:
    strict-casts: true
    strict-inference: true
    strict-raw-types: true
