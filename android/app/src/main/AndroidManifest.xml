<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.WAKE_LOCK"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK"/>
    <!-- B-07: downloads need a dataSync FGS to survive Doze/App Standby; Android 14+ requires typed permission -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC"/>
    <!-- Android 15 (API 35+) prefers mediaProcessing for heavy media work (audio encode/tag/publish);
         the download FGS selects this type at runtime on API 35+ and dataSync on API 34. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PROCESSING"/>
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO"/>
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES"/>
    <uses-permission android:name="android.permission.READ_MEDIA_VISUAL_USER_SELECTED"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32"/>
    <!-- WRITE_EXTERNAL_STORAGE removed: downloads are written to the app-specific
         external files dir and published via MediaStore (IS_PENDING on API 29+,
         MediaScanner indexing of app-specific files on API 28). Nothing in the
         app writes to shared storage directly on any API level. -->
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS"/>
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS"/>
    <!-- Bluetooth codec control: read and set A2DP codec (LDAC/aptX/AAC/SBC) like Developer Options.
         BLUETOOTH_CONNECT is required on API 31+; BLUETOOTH covers API 26-30.
         Neither permission enables microphone or any data collection. -->
    <uses-permission android:name="android.permission.BLUETOOTH"
        android:maxSdkVersion="30"/>
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT"
        android:minSdkVersion="31"/>
    <uses-permission android:name="android.permission.WRITE_SETTINGS" tools:ignore="ProtectedPermissions"/>
    <!-- Play Console Policy Justification: android.permission.RECORD_AUDIO is required strictly for the
         hardware-accelerated live audio visualizer DSP pipeline (android.media.audiofx.Visualizer) to render
         real-time frequency spectrum waveforms. It is NEVER used to capture microphone audio, record user
         conversations, or transmit audio data off-device. It is requested at runtime ONLY when the user opens
         the visualizer screen; if denied, Pulsr gracefully falls back to synthetic simulated waveforms. -->
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>
    <uses-feature android:name="android.hardware.usb.host" android:required="false"/>
    <uses-feature android:name="android.hardware.microphone" android:required="false"/>

    <!-- Custom signature permission to protect internal widget & playback broadcasts -->
    <permission android:name="com.pulsr.music.permission.WIDGET_CONTROL"
        android:protectionLevel="signature" />
    <uses-permission android:name="com.pulsr.music.permission.WIDGET_CONTROL" />

    <!-- allowBackup is intentionally disabled (false) to prevent automated ADB or
         cloud backup extraction of cached audio, offline database, and user auth sessions/tokens. -->
    <application
        android:label="${appName}"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:allowBackup="false"
        android:supportsRtl="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:enableOnBackInvokedCallback="true"
        tools:replace="android:allowBackup">

        <meta-data
            android:name="com.google.android.gms.car.application"
            android:resource="@xml/automotive_app_desc" />
        <activity
            android:name="com.pulsr.music.MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
                <category android:name="android.intent.category.APP_MUSIC"/>
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.MUSIC_PLAYER" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.APP_MUSIC" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.media.action.MEDIA_PLAY_FROM_SEARCH" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:scheme="pulsrWidget" />
            </intent-filter>
            <!-- Unified intent filter to view/play audio files from file managers, downloads, etc. (BUG-012) -->
            <intent-filter android:label="${appName}">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="file" />
                <data android:scheme="content" />
                <data android:mimeType="audio/*" />
                <data android:mimeType="application/ogg" />
                <data android:mimeType="application/x-ogg" />
                <data android:mimeType="application/flac" />
                <data android:mimeType="application/x-flac" />
                <data android:mimeType="application/x-matroska" />
            </intent-filter>
            <!-- Intent filter to receive shared text/audio/URLs via Android share sheet -->
            <intent-filter android:label="${appName}">
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/plain" />
                <data android:mimeType="text/*" />
                <data android:mimeType="audio/*" />
            </intent-filter>
        </activity>
        
        <service android:name="com.ryanheise.audioservice.AudioService"
            android:foregroundServiceType="mediaPlayback"
            android:exported="true"
            tools:ignore="Instantiatable">
            <intent-filter>
                <action android:name="android.media.browse.MediaBrowserService" />
            </intent-filter>
        </service>

        <!-- B-07: Dedicated foreground service for YouTube downloads.
             Keeps downloads alive when app is backgrounded / screen off.
             Shows progress notification with pause/cancel actions; auto-stops when queue drains.
             Runtime selection (DownloadService.ensureForeground): API 35+ -> mediaProcessing,
             API 34 -> dataSync, below -> plain startForeground. The mediaPlayback type on the
             audio_service AudioService above is intentionally untouched. -->
        <service android:name="com.pulsr.music.DownloadService"
            android:foregroundServiceType="dataSync|mediaProcessing"
            android:exported="false"
            android:stopWithTask="false" />

        <receiver android:name="androidx.media.session.MediaButtonReceiver"
            android:exported="true" tools:ignore="Instantiatable">
            <intent-filter>
                <action android:name="android.intent.action.MEDIA_BUTTON" />
            </intent-filter>
        </receiver>

        <receiver
            android:name="com.pulsr.music.NowPlayingWidget"
            android:label="${appName} Widget"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_info" />
        </receiver>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <package android:name="com.google.android.gms" />
    </queries>
</manifest>
