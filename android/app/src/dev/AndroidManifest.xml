<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application>
        <!-- Google Cast (dev flavor). Uses the public Default Media Receiver. -->
        <meta-data
            android:name="com.google.android.gms.cast.framework.OPTIONS_PROVIDER_CLASS_NAME"
            android:value="com.pulsr.music.CastOptionsProvider" />
        <activity
            android:name=".MainActivity">
            <!-- Intent filter to open YouTube Music & YouTube links (dev/ytm flavors only) -->
            <intent-filter android:label="${appName}">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="http" />
                <data android:scheme="https" />
                <data android:host="music.youtube.com" />
                <data android:host="www.youtube.com" />
                <data android:host="youtube.com" />
                <data android:host="m.youtube.com" />
                <data android:host="youtu.be" />
            </intent-filter>
            <!-- Intent filter for shared YouTube / YouTube Music links (Share via Pulsr) -->
            <intent-filter android:label="${appName}">
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/plain" />
            </intent-filter>
        </activity>
    </application>
</manifest>
